<UserControl x:Class="MusicOre.Views.Player"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" DataContext="{Binding Source={StaticResource Locator}, Path=Player}"
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Button Width="70" Height="20" Content="Select" Command="{Binding Select}"></Button>
		<MediaElement x:Name="MediaElement" Width="0" Height="0" LoadedBehavior="Manual" UnloadedBehavior="Stop" ></MediaElement>
        <StackPanel Height="50" Orientation="Horizontal" Margin="60,188,-60,62">
            <Button Content="Previous" Command="{Binding Previous}"/>
            <Button Content="Play" />
            <Button Content="Next" Command="{Binding Next}"/>
        </StackPanel>
    </Grid>
</UserControl>
